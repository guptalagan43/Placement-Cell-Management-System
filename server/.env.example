# Server environment configuration. Copy to .env for local development.
# In production, set these as real environment variables (never commit secrets).

# Runtime environment: development | test | production
NODE_ENV=development

# Port the API listens on
PORT=5000

# MongoDB connection string (MongoDB Atlas in deployment). Required.
MONGODB_URI=mongodb://localhost:27017/pcms
